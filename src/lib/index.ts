// place files you want to import through the `$lib` alias in this folder.
let IconContext = {

    IconObJ(){
        let newIcon:any;

        return{
            getNewIcon(){
                return newIcon;
            },
            setNewIcon(value:any){
                newIcon = value;
            }
        } 
    },
    
    createIcon(){
        const aIcon = document
    .createElement('i');
    }

    
}

